#!/bin/sh
#SBATCH --partition=GPUQ
#SBATCH --gres=gpu:1
#SBATCH --constraint={{ gpu.lower() }}
#SBATCH --account=share-ie-idi
#SBATCH --time=32:00:00
#SBATCH --nodes=1
#SBATCH -c 4
#SBATCH --mem=12000
#SBATCH --job-name="{{ run_number }}-{{ model_name }}-{{ dataset_name }}-{{ hpc_cluster }}-{{ gpu }}-{{ machine_learning_framework }}-{{ container_short }}"
#SBATCH --output=out.{{ run_number }}
#SBATCH --mail-user=kevincoa@stud.ntnu.no
#SBATCH --mail-type=ALL
 

WORKDIR=${SLURM_SUBMIT_DIR}
cd ${WORKDIR}
echo "we are running from this directory: $SLURM_SUBMIT_DIR"
echo " the name of the job is: $SLURM_JOB_NAME"
echo "Th job ID is $SLURM_JOB_ID"
echo "The job was run on these nodes: $SLURM_JOB_NODELIST"
echo "Number of nodes: $SLURM_JOB_NUM_NODES"
echo "We are using $SLURM_CPUS_ON_NODE cores"
echo "We are using $SLURM_CPUS_ON_NODE cores per node"
echo "Total of $SLURM_NTASKS cores"

export OS_AUTH_TYPE=v3applicationcredential
export OS_AUTH_URL=https://identity.cloud.sdsc.edu:5000/v3
export OS_IDENTITY_API_VERSION=3
export OS_REGION_NAME="SDSC"
export OS_INTERFACE=public
export OS_APPLICATION_CREDENTIAL_ID=<UPDATE>
export OS_APPLICATION_CREDENTIAL_SECRET=<UPDATE>

module purge
module list

pwd

# /cluster/home/kevincoa

cd /cluster/work/kevincoa/project-02/code/image_classification/

singularity run --nv --bind /cluster/work/kevincoa:/cluster/work/kevincoa /cluster/work/kevincoa/singularity/{{ container_filename }} nvidia-smi

singularity run --nv --bind /cluster/work/kevincoa:/cluster/work/kevincoa /cluster/work/kevincoa/singularity/{{ container_filename }} bash /cluster/work/kevincoa/project-02/code/image_classification/seed-wrapper-{{ run_number }}.sh {{ model_name }}-{{ dataset_name.lower() }}-{{ hpc_cluster.lower() }}-{{ gpu.upper() }}-{{ machine_learning_framework }}-{{ container_short.lower() }} {{ machine_learning_framework }} {{ model_name }} {{ dataset_name.lower() }} {{ optimizer}} {{ epochs }} {{ batch_size }} {{ learning_rate }} {{ lr_scheduler }} {{ lr_warmup }}